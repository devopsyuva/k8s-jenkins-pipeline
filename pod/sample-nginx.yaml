apiVersion: v1
kind: Pod
metadata:
  name: nginx-demo
  labels:
    name: nginx-demo
spec:
  containers:
  - name: nginx-c1
    image: nginx
    ports:
    - containerPort: 80
      name: http
      protocol: TCP
